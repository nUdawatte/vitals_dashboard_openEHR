import { type EventEmitter } from '../../internal/decorators';
import EhrElement from '../EhrElement';
interface Quantity {
    magnitude?: number;
    unit?: string;
}
export default class QuantityElement extends EhrElement {
    data: Quantity | undefined;
    _mbInput: EventEmitter<Quantity>;
}
export {};
