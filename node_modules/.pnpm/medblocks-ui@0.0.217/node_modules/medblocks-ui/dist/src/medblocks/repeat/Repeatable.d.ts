import { LitElement } from 'lit-element';
import { type EventEmitter } from '../../internal/decorators';
export declare function getRepeatableRegex(path: string): RegExp;
export default class Repeatable extends LitElement {
    isRepeatable: boolean;
    count: number;
    path: string;
    _mbCount: EventEmitter<number>;
    _mbConnectRepeatable: EventEmitter<string>;
    get regex(): RegExp;
    connectedCallback(): void;
    handleCountChange(_: number, newCount: number): void;
}
