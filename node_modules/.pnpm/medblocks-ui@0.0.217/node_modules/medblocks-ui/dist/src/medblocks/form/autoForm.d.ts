import { LitElement } from 'lit-element';
import { type MBComposition } from './autoform-utils';
import type { SearchFunction } from '../codedtext/searchFunctions';
export declare class MedblocksAutoForm extends LitElement {
    static styles: import("lit-element").CSSResult;
    templateId: string;
    ctx: {};
    config: {};
    webTemplate: undefined;
    variant: string;
    addContext: boolean;
    handleSearch: SearchFunction;
    handleInput(event: CustomEvent): void;
    firstUpdated(): void;
    generateAutoForm(): void;
    import(composition: MBComposition): void;
    clear(): void;
    bindValue(composition: MBComposition): void;
    export(): any;
    submit(): any;
    render(): import("lit-element").TemplateResult;
}
