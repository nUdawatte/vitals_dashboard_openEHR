import type { AxiosInstance } from 'axios';
export interface MediaOptions {
    axios: AxiosInstance;
    file: FormData;
}
export interface MediaResult {
    Key: string;
}
type URI = string;
export type MediaFunction = (options: MediaOptions) => Promise<MediaResult>;
export interface StoragePlugin {
    upload(options: {
        file: File;
        axios: AxiosInstance;
    }): Promise<URI>;
    download(options: {
        axios: AxiosInstance;
        key: URI | undefined;
    }): Promise<URI>;
}
export declare const supabaseStoragePlugin: StoragePlugin;
export {};
