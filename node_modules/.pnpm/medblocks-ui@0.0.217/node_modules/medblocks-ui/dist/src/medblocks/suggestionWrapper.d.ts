import { LitElement } from 'lit-element';
import '@shoelace-style/shoelace/dist/components/tag/tag';
import { type EventEmitter } from '../internal/decorators';
export interface Suggestion {
    data: any;
    label: string;
    op: 'replace' | 'add' | 'remove';
}
export interface SuggestEvent {
    suggestion: Suggestion;
    path: string;
    global: boolean;
}
export default class SuggestWrapper extends LitElement {
    static styles: import("lit-element").CSSResult;
    _suggestionEvent: EventEmitter<SuggestEvent>;
    _handleSuggestion(suggestion: Suggestion): void;
    path: string;
    suggestions: Suggestion[];
    global: boolean;
    label: string;
    render(): import("lit-element").TemplateResult;
}
